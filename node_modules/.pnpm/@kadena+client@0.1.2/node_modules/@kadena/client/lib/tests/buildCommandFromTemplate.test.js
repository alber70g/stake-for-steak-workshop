"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const buildCommandFromTemplate_1 = require("../buildCommandFromTemplate");
describe('buildCommandFromTemplate', () => {
    it('merges parts, holes and args correctly', () => {
        const parts = [`This is a template for `, `.`];
        const holes = ['name'];
        const args = { name: 'Albert' };
        const expected = 'This is a template for Albert.';
        const cmd = (0, buildCommandFromTemplate_1.buildCommandFromTemplate)(parts, holes, args);
        expect(cmd).toBe(expected);
    });
});
//# sourceMappingURL=buildCommandFromTemplate.test.js.map